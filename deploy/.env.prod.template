# ============================================================
# Eventron — Production .env (auto-generated by CI/CD)
# DO NOT edit on server — change GitHub Secrets or this template
# ============================================================

# ── Database ─────────────────────────────────────────────────
POSTGRES_USER=eventron
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_DB=eventron
DATABASE_URL=postgresql+asyncpg://eventron:${POSTGRES_PASSWORD}@postgres:5432/eventron

# ── Redis ─────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── LLM ──────────────────────────────────────────────────────
LLM_DEFAULT_TIER=smart

DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
DEEPSEEK_MODEL=deepseek-chat

OPENAI_API_KEY=${OPENAI_API_KEY}
OPENAI_MODEL=gpt-4o

ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
ANTHROPIC_MODEL=claude-sonnet-4-6

GLM_API_KEY=${GLM_API_KEY}
GLM_MODEL=glm-4-flash

QWEN_API_KEY=${QWEN_API_KEY}
QWEN_ACCESS_KEY_SECRET=${QWEN_ACCESS_KEY_SECRET}
QWEN_MODEL=qwen-plus

# ── WeChat Work ──────────────────────────────────────────────
WECOM_CORP_ID=wwa885288db03f8092
WECOM_BOT_SECRET=${WECOM_BOT_SECRET}
WECOM_BOT_TOKEN=${WECOM_BOT_TOKEN}
WECOM_BOT_AES_KEY=${WECOM_BOT_AES_KEY}

# ── Auth ─────────────────────────────────────────────────────
JWT_SECRET=${JWT_SECRET}

# ── Plugins ──────────────────────────────────────────────────
PLUGIN_IDENTITY=true
PLUGIN_SEATING=true
PLUGIN_CHECKIN=true
PLUGIN_CHANGE=true
PLUGIN_PAGEGEN=true
PLUGIN_BADGE=true
PLUGIN_GUIDE=true

# ── Runtime ──────────────────────────────────────────────────
DEBUG=false
DB_ECHO=false
VITE_PUBLIC_URL=https://event.bheartmedia.com
